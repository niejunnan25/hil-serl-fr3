"""Reference marker for the DROID ZED port.

This package exists only as a static reference for diffing our local ZED
capture path against the upstream DROID implementation living on
``fr3-desktop-ts:/home/robot/droid``. It is **not** a runtime dependency of
the hilserl-fr3 training pipeline and should never be imported from any
production capture code.
"""
