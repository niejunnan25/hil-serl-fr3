# RAM Insertion

Owner: Codex primary, Human approval required before live robot use

This task folder mirrors the upstream HIL-SERL RAM insertion example as a
project-local experiment folder. It is useful as the closest upstream precedent
for precise insertion.
