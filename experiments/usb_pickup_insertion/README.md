# USB Pickup Insertion

Owner: Codex primary, Human approval required before live robot use

This task folder mirrors the upstream HIL-SERL USB pickup/insertion example as a
project-local experiment folder. Keep task data, checkpoints, classifier data,
and edited config inside this folder.
